import styled from "styled-components";

export const StNumberComma = styled.span`
  margin-top: 2px;
  font-size: ${(props) => props.size};
  line-height: ${(props) => props.lineHeight};
  font-weight: ${(props) => props.weight};
  font-family: "Inter", sans-serif;
`;
