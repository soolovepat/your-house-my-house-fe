import styled, { css } from "styled-components";
import theme from "../../../styles/theme";

export const StItemList = styled.div`
  p {
    font-size: 14px;
    color: ${theme.darkGrayColor};
    margin: 30px 0 10px;
  }
`;
